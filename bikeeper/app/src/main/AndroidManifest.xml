<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.techtown.mybike">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_bikeeper"
        android:label="bikeeper"
        android:roundIcon="@color/MyBikeColor"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyCFktMprZhqWvYGyB8dofs-rPXXefY7T5I" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pushActivity" />
        <activity android:name=".LocationActivity" />
        <activity android:name=".TimeSelectActivity" />
        <activity android:name=".CompleteActivity" />
        <activity android:name=".BikeStateActivity" />
        <activity android:name=".LocationRetrunActivity" />
        <activity android:name=".TimeSelectReturnActivity" />
        <activity android:name=".CompleteReturnActivity" />
        <activity android:name=".LockPopupAnimActivity" />
        <activity
            android:name=".SecuritySetActivity"
            android:theme="@style/Theme.AppCompat.Dialog" />
        <activity android:name=".InquiryActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".ReservationReturnActivity" />
        <activity android:name=".ReservationStoreActivity" />
        <activity android:name=".MyInfoActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".BikeInfoAcivity" />
        <activity android:name=".Personalinfo2Activity" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".LoadingActivity"></activity>
    </application>

</manifest>